# 4. Пользователь вводит строку из нескольких слов, разделённых пробелам
import re

my_centence = input ("Введите строку из нескольких слов: ")

my_list = re.split('\s+',my_centence)
for w in my_list:
    print (w[:10],'\n')
